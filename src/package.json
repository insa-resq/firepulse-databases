{
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=22.19.0"
    },
    "scripts": {
        "db:generate": "prisma generate",
        "db:migration:create": "prisma migrate dev --create-only",
        "db:migration:deploy": "prisma migrate deploy",
        "db:reset": "prisma migrate reset --force",
        "db:seed": "prisma db seed",
        "db:validate": "prisma validate",
        "db:uml": "node ./scripts/generate-uml.js"
    },
    "dependencies": {
        "@mrleebo/prisma-ast": "^0.13.0",
        "dotenv": "17.2.3",
        "prisma": "^7.1.0"
    },
    "devDependencies": {
        "@faker-js/faker": "^10.1.0",
        "@prisma/adapter-pg": "^7.1.0",
        "@prisma/client": "^7.1.0",
        "@types/bcrypt": "^6.0.0",
        "@types/node": "^25.0.0",
        "bcrypt": "^6.0.0",
        "tsx": "^4.21.0"
    }
}
