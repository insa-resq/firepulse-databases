datasource firepulse_db {
    provider     = "postgresql"
    relationMode = "foreignKeys"
    schemas      = ["accounts", "detection", "planning", "registry"]
}

generator client {
    provider     = "prisma-client"
    output       = "../prisma-client"
    moduleFormat = "esm"
}
